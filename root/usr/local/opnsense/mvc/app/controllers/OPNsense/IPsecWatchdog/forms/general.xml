<form>
    <field>
        <id>general.webhookUrl</id>
        <label>Webhook URL</label>
        <type>text</type>
        <help>Called when a tunnel is still down after the number of consecutive reconnect attempts set below. Any HTTP(S) endpoint works - point it at a Slack/Discord incoming webhook, PagerDuty, your own receiver, etc. Leave blank to disable notifications for any tunnel that doesn't set its own webhook URL below.</help>
    </field>
    <field>
        <id>general.webhookAttempts</id>
        <label>Notify after this many failed attempts</label>
        <type>text</type>
        <help>Consecutive reconnect attempts (not minutes) a tunnel must fail before the webhook fires. Resets once the tunnel comes back up, so it can fire again on a future outage.</help>
    </field>
    <field>
        <id>general.webhookSecret</id>
        <label>Webhook signing secret (optional)</label>
        <type>password</type>
        <help>If set, every webhook request includes an X-Watchdog-Signature header (hex-encoded HMAC-SHA256 of the request body, using this secret) so a receiver can verify it actually came from this plugin.</help>
    </field>
    <field>
        <id>general.notifyOnDown</id>
        <label>Notify when a tunnel goes down</label>
        <type>checkbox</type>
        <help>Fires immediately the first time a tunnel is detected down, before any reconnect attempt is even made.</help>
    </field>
    <field>
        <id>general.notifyOnStuck</id>
        <label>Notify when still down after failed attempts</label>
        <type>checkbox</type>
        <help>Fires once a tunnel has failed the "notify after this many attempts" count above. This is the original/default alert.</help>
    </field>
    <field>
        <id>general.notifyOnUp</id>
        <label>Notify when a tunnel comes back up</label>
        <type>checkbox</type>
        <help>Fires once, when a tunnel that had been tracked as down recovers - whether or not it ever reached the other notifications above.</help>
    </field>
</form>
